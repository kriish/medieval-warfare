// player name also acts as a globally unique id
var playerName = false;

// hash by player name, stores pointer to players Kingdom
var players = false;

function Kingdom(player) {
var troops = false;
var cities = false;

    function addCity(city) {
       cities[city.name] = city;
    }
    function addTroop(troop) {
       troops[troop.name] = troop;
    }
}
